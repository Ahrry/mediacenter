<Page x:Class="mediacenter.Views.DetailFilm"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
	Title="DetailFilm">

    <Grid DataContext="{Binding SelectedVideo}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Libellés -->
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Nom"/>
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Nom"/>
        <TextBlock Grid.Row="2" Grid.Column="0" Text="Nom"/>
        <TextBlock Grid.Row="3" Grid.Column="0" Text="Nom"/>
        <TextBlock Grid.Row="4" Grid.Column="0" Text="Nom"/>

        <!-- Champs -->
        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Libelle}"/>
        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Libelle}"/>
        <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Libelle}"/>
        <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding Libelle}"/>
        <Button Command="{Binding SaveCommand}" >Save</Button>
        
        
    </Grid>
    
</Page>
