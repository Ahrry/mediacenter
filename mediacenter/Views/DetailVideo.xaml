<UserControl x:Class="mediacenter.Views.DetailVideo"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:UserControl="clr-namespace:mediacenter.UserControls"
             xmlns:Views="clr-namespace:mediacenter.Views"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="400">
    
    <Grid>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        
        <!-- Détails -->
        <ContentPresenter ContentTemplate="{Binding Detail}" DataContext="{Binding}" />
        
        <!-- Lecteur vidéo-->
        <UserControl:LecteurVideo Grid.Column="1" />

    </Grid>
    
</UserControl>
